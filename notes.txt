Simple form in Main.html

const product = {
  type: 'makeup',
  hasColor: true
};

if (product.type === makeup) {
  foundation
  eyeshadow
}

if (product.type === skincare) {
  facewash
  moisturizer
}

if (product.type === hairdye) {
  strength
}

name

if (product.hasColor) {
  color
}

color:
Text box (brand name) 
Hexadecimal/color picker approximation
